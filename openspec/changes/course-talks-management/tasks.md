# Tasks: course-talks-management

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 3,000–5,500 across schema, domain, services, UI, integrations, and tests |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 domain foundation → PR 2 grading/eligibility → PR 3 certificates/PDF/QR → PR 4 commercial documents/IGV → PR 5 delivery/history → PR 6 UI workflows → PR 7 dashboards/alerts/audit/rollout |
| Delivery strategy | ask-on-risk → chained delivery approved |
| Chain strategy | stacked-to-main (approved) |

Decision needed before apply: No — chained delivery approved
Chained PRs recommended: Yes
Chain strategy: stacked-to-main (approved)
400-line budget risk: High

> The human approved chained delivery. Implement each slice as a separate, reviewable branch/PR targeting the immediately preceding slice (Slice 1 targets the base branch), with tests kept with the behavior. Do not combine later-slice surfaces into an earlier branch.

## Slice 0 — Apply planning gate and branch boundary

- [x] Confirm chain strategy: `stacked-to-main` approved for chained delivery. <!-- sdd-owner: parent -->
- [ ] Start or reuse a bounded review context for Slice 1 after implementation completes, verifying the slice diff excludes later-slice surfaces. <!-- sdd-owner: parent -->

## Slice 1 — Domain schema, enums, models, factories, permissions; no menu exposure

**Edit surfaces:** `database/migrations/*course*.php`, `database/seeders/*Permission*`, `app/Enums/Courses/*`, `app/Models/Courses/*`, `database/factories/Courses/*`, `app/Policies/Courses/*`, `app/Providers/AuthServiceProvider.php` or equivalent policy registration, `config/courses.php`, `tests/Feature/Courses/CourseDomainFoundationTest.php`, `tests/Unit/Courses/*Enum*Test.php`.

- [x] RED: add failing unit/feature coverage for activity code uniqueness, modality required fields, edition state transitions, enrollment uniqueness, permission denial stubs, and migration-level model relationships. <!-- sdd-owner: implementation -->
- [x] GREEN: create migrations for `course_activities`, `course_editions`, `course_edition_teachers`, `course_sessions`, `course_participants`, `course_enrollment_groups`, `course_enrollments`, `course_attendances`, `course_grades`, `course_certificate_templates`, `course_academic_documents`, and `course_commercial_documents` using string states, indexes, soft deletes where designed, and non-destructive rollbacks. <!-- sdd-owner: implementation -->
- [x] GREEN: add `App\Enums\Courses` backed enums for activity type, edition state, modality, enrollment state, payment status, final result, academic document type/status, delivery status, and commercial document type. <!-- sdd-owner: implementation -->
- [x] GREEN: add `App\Models\Courses` Eloquent models, relations, casts, `HasAuditColumns`, `LogsActivity`, factories, and basic policies for the permission names from `openspec/changes/course-talks-management/design.md`. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add tests for duplicate enrollment per edition/participant, group payer with multiple academic records, and permission names seeded/assignable under Spatie permission. <!-- sdd-owner: implementation -->
- [x] REFACTOR: centralize constants/config defaults in `config/courses.php` for `igv_rate=0.18`, `delivery_due_days=1`, default currency `PEN`, and avoid leaking menu/routes before later UI slices. <!-- sdd-owner: implementation -->
- [x] Run focused verification with `php artisan test --filter=CourseDomainFoundationTest` plus affected unit enum tests. <!-- sdd-owner: implementation -->
- [ ] Review Slice 1 for migration safety, rollback behavior, policy registration, and absence of user-facing route/menu changes. <!-- sdd-owner: parent -->

## Slice 2 — Activity, edition, enrollment, attendance, grades, and eligibility services

**Depends on:** Slice 1. **Edit surfaces:** `app/Services/Courses/CourseActivityService.php`, `CourseEditionService.php`, `CourseEnrollmentService.php`, `CourseAttendanceService.php`, `CourseGradeService.php`, `CourseGradeCalculator.php`, `CourseEligibilityService.php`, `app/Events/Courses/*`, `app/Jobs/Courses/EvaluateCourseDocumentEligibility.php`, `tests/Unit/Courses/CourseGradeCalculatorTest.php`, `tests/Feature/Courses/CourseEligibilityTest.php`, `tests/Feature/Courses/CourseAttendanceAndGradesTest.php`.

- [x] RED: add grade calculator boundary tests for `12.49 -> 12 Participación`, `12.50 -> 13 Aprobado`, `12.60 -> 13 Aprobado`, equal weights, two-decimal display, and no float drift. <!-- sdd-owner: implementation -->
- [x] RED: add feature tests proving talks reject grades, course attendance is informative, talk participation gates eligibility, unpaid/invalid participants do not generate eligibility, and completed courses select approval vs participation result. <!-- sdd-owner: implementation -->
- [x] GREEN: implement activity/edition services with code uniqueness, modality validation, teacher/session management, state transition guards, and audit events under `course-*`. <!-- sdd-owner: implementation -->
- [x] GREEN: implement enrollment and attendance services for participant linking/creation, group payer records, per-participant academic records, payment status changes, attendance marking, and eligibility trigger events after commit. <!-- sdd-owner: implementation -->
- [x] GREEN: implement grade service and pure grade calculator using integer/decimal-string arithmetic or `bc*` functions; persist exact average, display average, rounded result, final result, and correction audit. <!-- sdd-owner: implementation -->
- [x] GREEN: implement eligibility service returning explicit missing conditions for payment, participant data, edition validations, academic result, and participation; implement idempotent `EvaluateCourseDocumentEligibility` job as a no-op until document generation exists. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add tests for payment `paid`/authorized `waived` complete, `partial`/`refunded` incomplete, withdrawn/no-show ineligible, and validation completion as the final trigger. <!-- sdd-owner: implementation -->
- [x] REFACTOR: keep controllers absent/thin in this slice; ensure services own transitions and can be called from future UI/jobs without duplicated rules. <!-- sdd-owner: implementation -->
- [x] Run focused verification with `php artisan test --filter=CourseGradeCalculatorTest` and `php artisan test --filter=CourseEligibilityTest`. <!-- sdd-owner: implementation -->
- [x] Review Slice 2 for strict TDD evidence, arithmetic correctness, event-after-commit behavior, and no premature PDF/delivery/UI implementation. <!-- sdd-owner: parent -->

## Slice 3 — Certificate template, reference PDF, QR security, revocation/regeneration

**Depends on:** Slices 1–2. **Edit surfaces:** `composer.json`/`composer.lock` if a QR package is chosen, `app/Services/Courses/CourseDocumentGenerationService.php`, `app/Services/Courses/CertificateQrTokenService.php`, `app/ViewModels/Courses/CourseCertificateViewModel.php`, `resources/views/course-talks/certificates/reference.blade.php`, `routes/web.php`, `app/Http/Controllers/CourseTalks/PublicCertificateQrController.php`, `app/Services/DocumentService.php`, `tests/Feature/Courses/CourseAcademicDocumentGenerationTest.php`, `tests/Feature/Courses/CourseCertificateQrSecurityTest.php`, `tests/Unit/Courses/CourseCertificateFilenameTest.php`.

- [x] RED: add failing tests for automatic document type selection, filename pattern, PDF required reference sections including temario/signatures/QR/code, private document storage, unique token hashing, QR streams only current PDF, revoked/replaced token denied with no identity data, and regeneration requiring a reason. <!-- sdd-owner: implementation -->
- [x] GREEN: add or adapter-wrap a QR generator dependency, keeping the implementation behind `CertificateQrTokenService`/renderer interfaces so future package changes do not affect domain services. <!-- sdd-owner: implementation -->
- [x] GREEN: implement template model/service defaults and `resources/views/course-talks/certificates/reference.blade.php` to reproduce the approved reference-template structure with configurable admin fields but mandatory business data. <!-- sdd-owner: implementation -->
- [x] GREEN: implement `CourseDocumentGenerationService` to choose approval/participation/talk document type, generate code/token, render DomPDF, store private PDFs under `course-academic-documents/{enrollment_id}/{code}.pdf`, register `documents`, and create `course_academic_documents`. <!-- sdd-owner: implementation -->
- [x] GREEN: expand `app/Services/DocumentService.php` subject allow-list/assertion to support `CourseAcademicDocument` and later `CourseCommercialDocument` without weakening private-document checks. <!-- sdd-owner: implementation -->
- [x] GREEN: add public `GET /certificate/qr/{token}` route/controller that hashes the token with app key, streams only current non-revoked PDFs, rate-limits if existing route middleware supports it, and returns generic invalid/not-current responses. <!-- sdd-owner: implementation -->
- [x] GREEN: implement annul/regenerate service paths requiring permission/reason, preserving old private PDF for audit, revoking QR, marking old rows annulled/replaced, and creating a new current document with new code/token/status. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: test multiple certificates for one enrollment across regeneration, token uniqueness, raw token not persisted, and no public exposure of document number/email/phone/grades in invalid QR responses. <!-- sdd-owner: implementation -->
- [x] REFACTOR: isolate PDF/QR adapters for fakes in tests and keep generated assets out of public storage/symlinks. <!-- sdd-owner: implementation -->
- [x] Run focused verification with `php artisan test --filter=CourseAcademicDocumentGenerationTest` and `php artisan test --filter=CourseCertificateQrSecurityTest`. <!-- sdd-owner: implementation -->
- [x] Review Slice 3 for privacy, QR revocation, private storage, filename compliance, and reference-template completeness. <!-- sdd-owner: parent -->

## Slice 4 — Commercial documents, IGV, uploads, and no-tax-generation boundary

**Depends on:** Slices 1–3 for document integration. **Edit surfaces:** `app/Services/Courses/CourseCommercialDocumentService.php`, `app/Http/Requests/CourseTalks/*CommercialDocument*Request.php`, `app/Models/Courses/CourseCommercialDocument.php`, `app/Services/DocumentService.php`, `tests/Unit/Courses/CourseCommercialDocumentMoneyTest.php`, `tests/Feature/Courses/CourseCommercialDocumentRegistrationTest.php`.

- [x] RED: add tests for IGV calculation `100.00 + 20.00 = subtotal 120.00, IGV 21.60, total 141.60`, factura/boleta vs recibo policy, stored used rate, PEN default, group-or-enrollment constraint, metadata fields, attachment storage, and no SUNAT/accounting side effects. <!-- sdd-owner: implementation -->
- [x] GREEN: implement money calculation using decimal-safe half-up arithmetic from `config/courses.php` rate and persist subtotal, IGV rate/amount, total, currency, payer, series/number, issue date, observations, and `document_id`. <!-- sdd-owner: implementation -->
- [x] GREEN: implement external factura/boleta/recibo registration and upload integration via existing private `documents` storage, supporting group purchase or one enrollment while preserving per-participant academic records. <!-- sdd-owner: implementation -->
- [x] GREEN: add request validation classes for commercial document registration/upload surfaces to be used by later controllers. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: add tests for discounts/certificate charge included in taxable subtotal, invalid negative totals, missing file when status requires file, and document replacement audit. <!-- sdd-owner: implementation -->
- [x] REFACTOR: keep commercial document delivery status independent from payment completion and academic document validity. <!-- sdd-owner: implementation -->
- [x] Run focused verification with `php artisan test --filter=CourseCommercialDocument`. <!-- sdd-owner: implementation -->
- [ ] Review Slice 4 for IGV/commercial document correctness, private upload safety, and explicit v1 non-goal of tax-document generation. <!-- sdd-owner: parent -->

## Slice 5 — Email delivery, WhatsApp assisted handoff, delivery history, snapshots

**Depends on:** Slices 3–4. **Edit surfaces:** `app/Services/Courses/CourseDocumentDeliveryService.php`, `app/Jobs/Courses/SendCourseDocumentEmail.php`, `app/Events/Courses/CourseDocumentDeliveryAttempted.php`, `app/Listeners/Courses/*Delivery*`, `app/Models/Notification/OutboundDelivery.php` helpers if needed, `tests/Feature/Courses/CourseDocumentEmailDeliveryTest.php`, `tests/Feature/Courses/CourseDocumentWhatsAppDeliveryTest.php`.

- [x] RED: add tests for email success marking sent and `last_sent_at`, email failure keeping pending/failed with visible error, resend appending history, WhatsApp open creating a handoff entry but keeping pending, manual `Marcar como enviado` marking sent, and recipient override persistence. <!-- sdd-owner: implementation -->
- [x] GREEN: implement delivery service for academic and commercial documents using `outbound_deliveries` append-only rows keyed to related entity, operation idempotency keys, status snapshots, responsible user, recipient, channel, attempts, and error fields. <!-- sdd-owner: implementation -->
- [x] GREEN: implement `SendCourseDocumentEmail` job wrapping existing `App\Services\Email\EmailService` with attachments or secure links as designed; update snapshots only on recorded success. <!-- sdd-owner: implementation -->
- [x] GREEN: implement WhatsApp assisted URL builder using `wa.me`/WhatsApp Web prepared text plus secure document link, explicitly avoiding automatic `WhatsAppService` dispatch in v1. <!-- sdd-owner: implementation -->
- [x] GREEN: implement manual WhatsApp confirmation path requiring actor, recipient, timestamp, and delivery-history append before status changes to sent. <!-- sdd-owner: implementation -->
- [x] TRIANGULATE: test commercial documents and academic documents share delivery behavior, failed resend history remains intact, and WhatsApp cannot be auto-marked sent merely by opening the handoff. <!-- sdd-owner: implementation -->
- [x] REFACTOR: extract channel-neutral delivery snapshot helpers and keep raw QR tokens/secrets out of logs and outbound-delivery payloads. <!-- sdd-owner: implementation -->
- [x] Run focused verification with `php artisan test --filter=CourseDocumentEmailDeliveryTest` and `php artisan test --filter=CourseDocumentWhatsAppDeliveryTest`. <!-- sdd-owner: implementation -->
- [ ] Review Slice 5 for email/WhatsApp v1 boundary, delivery history append-only behavior, and pending-state correctness. <!-- sdd-owner: parent -->

## Slice 6 — Authenticated routes, controllers, requests, and Blade UI workflows

**Depends on:** Slices 1–5. **Edit surfaces:** `routes/web.php`, `app/Http/Controllers/CourseTalks/*Controller.php`, `app/Http/Requests/CourseTalks/*Request.php`, `resources/views/course-talks/**/*`, `resources/views/layouts/*` or existing navigation partials for menu exposure, `tests/Feature/CourseTalks/*HttpTest.php`.

### Slice 6 units

The slice-level rows above are aggregate and cannot be checked until every workflow is delivered. Track progress through these units instead.

- [x] 6.a Activities and editions HTTP surfaces: authenticated `course-talks` route group, activity index/show/create/store, edition create/store/show with teacher and session management, form requests, and Blade views. Evidence: `CourseActivityCreateHttpTest`, `CourseEditionCreateHttpTest`, `CourseEditionSessionsHttpTest`, `CourseEditionTeachersHttpTest`, `CourseTalksReadOnlyHttpTest` — 63 tests / 370 assertions passing. <!-- sdd-owner: implementation -->
- [x] 6.b Enrollments and participants UI: per-edition enrollment list, participant linking and creation, group payer enrollment, and payment status display. Evidence: `CourseEnrollmentHttpTest` — 21 tests / 161 assertions passing; `--filter=Course` regression run 202 tests / 1,202 assertions passing. <!-- sdd-owner: implementation -->
- [x] 6.c Attendance matrix: per-session attendance marking for courses and informational attendance for talks. Evidence: `CourseAttendanceHttpTest` — 16 tests / 104 assertions passing; `--filter=Course` regression run 218 tests / 1,306 assertions passing. <!-- sdd-owner: implementation -->
- [x] 6.d Grade matrix: course grade recording and correction, with grades blocked for talks. Evidence: `tests/Feature/Courses/CourseGradeHttpTest.php` — 10 tests / 102 assertions passing; `--filter=Course` regression run 239 tests / 1,541 assertions passing; grade editing delegation proven through `CourseGradeService` with `course-talks.grades.manage` guarding both verbs. <!-- sdd-owner: implementation -->
- [ ] 6.e Academic document actions: generate, regenerate, annul, email, WhatsApp handoff, confirm sent, and discard. <!-- sdd-owner: implementation -->
- [x] 6.e-1 Academic document lifecycle UI (split from 6.e): per-edition document list with the expected document type and eligibility/missing conditions shown before acting, plus generate, regenerate (reason required) and annul (reason required, QR revoked). Gated by `course-talks.documents.generate` for generate/regenerate and `course-talks.documents.revoke` for annul. Evidence: `tests/Feature/Courses/CourseAcademicDocumentHttpTest.php` — 16 tests / 149 assertions passing; `--filter=Course` regression 255 tests / 1,690 assertions passing. NOTE: the aggregate 6.e row stays open because it also requires the 6.e-2 delivery actions (email, WhatsApp handoff, confirm sent) which are NOT delivered by 6.e-1. <!-- sdd-owner: implementation -->
- [x] 6.e-2 Academic document delivery actions (split from 6.e): per-document email (queued through `CourseDocumentDeliveryService::queueAcademicEmail()`, recipient prefilled from the participant's real email and editable, override persisted in the `outbound_deliveries` ledger), assisted WhatsApp handoff (phone prefilled from the participant's real mobile, browser redirected to the returned `wa.me` URL) and manual `Marcar como enviado` confirmation (actor, phone and matching handoff required), plus the per-document delivery history (channel, status, attempts, recipient reference, last update and recorded error). Every action is gated on `course-talks.documents.send` and the idempotency key comes from a hidden field minted once per rendered form. Evidence: `tests/Feature/Courses/CourseAcademicDocumentDeliveryHttpTest.php` — 15 tests / 170 assertions passing; `--filter=Course` regression 276 tests / 1,917 assertions passing. NOTE: the aggregate 6.e row stays open because its `discard` action is NOT delivered here (it belongs to Slice 7) — 6.e-1 plus 6.e-2 cover generate/regenerate/annul/email/WhatsApp handoff/confirm sent only. <!-- sdd-owner: implementation -->

> Correction note (corrective unit `certificate-qr-revoke-status-guard`, recorded in `apply-progress.md`): `CertificateQrTokenService::revoke()` now enforces the current-status guard inside the domain, re-reading the persisted status under a lock, so only a `Current` document can be annulled and a prior annulment's reason/actor/timestamp survive. The controller boundary check added by 6.e-1 is kept as defence in depth. No state changed above: 6.e-1 stays `[x]` and the aggregate 6.e row stays `[ ]` because 6.e-2 delivery actions are still missing.

> Correction note (corrective unit `academic-email-document-and-precondition`, recorded in `apply-progress.md`): the academic email now carries the document through the existing signed temporary/read route in a real Spanish body and a document-specific subject (the old one-line placeholder is gone), the emailed link uses a configurable days-long validity (`courses.email_document_link_minutes`, default 7 days) while the WhatsApp handoff keeps its 60 minutes, and the deliverability precondition (current status, QR not revoked, private file present) now lives in one private service predicate used by `queueAcademicEmail()`, `openAcademicWhatsAppHandoff()` and `secureAcademicDocumentUrl()` before any ledger row exists. The 6.e-2 controller's redundant `assertDeliverable()` workaround was removed rather than kept as defence in depth. No checkbox changed: 6.e-2 already reflects the delivered delivery actions, the aggregate 6.e row stays `[ ]` (its `discard` action belongs to Slice 7), 6.f stays `[ ]`, and every parent-owned row is untouched.
- [ ] 6.f Commercial document actions: register, upload, send, and discard, plus certificate template settings. <!-- sdd-owner: implementation -->
- [x] 6.f-1 Commercial document registration UI (split from 6.f): per-edition list of the registered facturas/boletas/recibos (type, payer, series/number, emission date, currency, service-computed subtotal / IGV rate / IGV / total, status, delivery status and whether a private attachment exists), registration for a SINGLE enrollment whose pre-submit breakdown comes from `CourseCommercialDocumentService::calculateCharges()` (factura and boleta at the configured 18% rate, recibo at 0) and persisted through `register()`, and the private attachment upload through `upload()`. Listing read gated by `CourseEditionPolicy::view`; registration and upload gated by `course-talks.commercial-documents.manage`, which the service itself re-asks for the actor. Every domain rejection (negative subtotal, both or neither target, invalid type, missing or disallowed file) surfaces as a visible Spanish error and never an HTTP 500. GAP: the group purchase path is NOT delivered — `calculateCharges()` takes the charges of ONE payer and no service method aggregates a group's enrollment charges, so no group breakdown can be produced without inventing domain math; `register()` still owns the exactly-one-target rule and refuses a group target posted to the enrollment endpoint. NOTE: the aggregate 6.f row stays open because commercial delivery actions (6.f-2), discard (Slice 7) and certificate template settings are NOT delivered by 6.f-1. Evidence: `tests/Feature/Courses/CourseCommercialDocumentHttpTest.php` — 16 tests / 188 assertions passing; `--filter=CourseCommercialDocument` 37 tests / 309 assertions passing; `--filter=Course` regression 297 tests / 2,140 assertions passing (same run without this class: 281 / 1,952, so the unit adds exactly its own 16 tests / 188 assertions). <!-- sdd-owner: implementation -->
- [x] 6.f-1b Group purchase aggregation, the zero-amount guard in `register()`, and the group registration UI (split from 6.f): `CourseCommercialDocumentService::calculateGroupCharges()` sums the charges of a group's billable enrollments (every state except the terminal `withdrawn`/`no_show`, per `design.md`) and delegates the tax math to `calculateCharges()`, refusing in Spanish a group with no billable enrollment or a zero aggregated subtotal; `register()` now takes a group target's money ONLY from that aggregation or from an explicit `subtotal_amount`, so a group can no longer be documented with a silently zero total, while the enrollment target and the explicit-subtotal path are unchanged; `POST course-talks/enrollment-groups/{group}/commercial-documents` (`commercial-documents.groups.store`) registers the group's comprobante with the payer prefilled from the group's own payer fields; the edition surface lists the edition's groups with the service-computed breakdown before submit and shows the service's reason, with no form, for a group that cannot be billed. Evidence: `tests/Feature/Courses/CourseCommercialDocumentRegistrationTest.php` — 12 tests / 56 assertions passing; `tests/Feature/Courses/CourseCommercialDocumentHttpTest.php` — 19 tests / 231 assertions passing; `--filter=CourseCommercialDocumentMoneyTest` 3 / 9 and `--filter=CourseCommercialDocumentDeliveryTest` 12 / 79 passing; `--filter=Course` regression 306 tests / 2,206 assertions passing. NOTE: the aggregate 6.f row stays open because commercial delivery actions (6.f-2), discard (Slice 7) and certificate template settings are still NOT delivered. <!-- sdd-owner: implementation -->

> Correction note (unit `6.f-1b`, recorded in `apply-progress.md`): the GAP stated on the 6.f-1 row above is now closed. `CourseCommercialDocumentService::calculateGroupCharges()` owns the group aggregation and `register()` no longer falls back to `'0'` charges for a group target — a zero-value group document is refused, not persisted. 6.f-1 keeps its own evidence unchanged: its enrollment-target behavior and the exactly-one-target rule are intact.
- [x] 6.f-2a Commercial email delivery domain path (split from 6.f-2): `CourseDocumentDeliveryService::queueCommercialEmail()` now builds a real message — a Spanish body carrying the signed temporary/read link from `secureCommercialDocumentUrl()` plus a document-specific subject (`Comprobante: Boleta B001-000123`) — and records the attempt as a `queued` ledger row correlated to the persisted `EmailMessage`, reusing the channel's single authorization / exactly-one-target / recipient / operation-key rules and the shared `courses.email_document_link_minutes` link validity. An undeliverable comprobante (not registered/sent, or its private file missing) is refused before any ledger row or queued message exists. The signed-URL builder's precondition is now the single predicate `hasStreamableCommercialDocument()` behind `assertStreamableCommercialDocument()`, used by `secureCommercialDocumentUrl()`, `queueCommercialEmail()` and `openCommercialWhatsAppHandoff()`. Evidence: `tests/Feature/Courses/CourseCommercialDocumentDeliveryTest.php` — 17 tests / 122 assertions passing; `--filter=Course` regression 311 tests / 2,249 assertions passing (baseline 306 / 2,206). NOTE: the aggregate 6.f row stays open because the commercial delivery UI (6.f-2b), discard (Slice 7) and certificate template settings are still NOT delivered — the commercial delivery UI depends on this domain path. <!-- sdd-owner: implementation -->

> Correction note (unit `6.f-2a`, recorded in `apply-progress.md`): the commercial channel had no queued email path — its only mail operation was the injected-closure stub (`static fn (): bool => true`) that marks a comprobante SENT without sending anything. `queueCommercialEmail()` is that real path, so the commercial delivery UI can be built onto a channel that actually sends. The stub-closure `sendAcademicEmail()`/`sendCommercialEmail()` pair is deliberately kept (this unit adds the real path beside it, it does not remove the injected-closure path), so the latent false-send risk remains wherever `app/` code injects the stub; it is reported in `apply-progress.md` and NOT fixed here. No checkbox changed except the new 6.f-2a row: `6.e` stays `[ ]`, `6.f` stays `[ ]`, and every parent-owned row is byte-for-byte unchanged.

- [x] 6.f-2b Commercial document delivery actions UI (split from 6.f-2): the edition's commercial documents screen now offers the three delivery actions per comprobante — email through the queued path `CourseDocumentDeliveryService::queueCommercialEmail()` (recipient prefilled from the enrollment's participant, or from the group's payer customer for a group comprobante, and editable; the override is persisted in the `outbound_deliveries` ledger and the message content carries the comprobante's signed link), assisted WhatsApp handoff (phone prefilled from the same real payer data; the browser is redirected to the returned `wa.me` URL and the delivery stays pending) and manual `Marcar como enviado` confirmation (actor, phone and matching handoff required) — plus the per-comprobante delivery history (channel, status, attempts, recipient reference, last update and recorded error). Every action is gated on the `send` ability of `CourseCommercialDocumentPolicy` (`course-talks.documents.send`, the same ability its routes and FormRequests ask for) and each idempotency key is a hidden field minted once per rendered form. `sendCommercialEmail()` is deliberately NOT wired: it would mark a comprobante sent without sending anything. Evidence: `tests/Feature/Courses/CourseCommercialDocumentDeliveryHttpTest.php` — 18 tests / 207 assertions passing; `--filter=Course` regression 329 tests / 2,456 assertions passing (baseline 311 / 2,249, so this unit adds exactly its own 18 tests / 207 assertions). Authorized extra surface: the read-only ledger query and the `group.payerCustomer` eager load added to `CourseCommercialDocumentController::index()` (no domain rule, no write). NOTE: the aggregate 6.f row stays open because discard (Slice 7) and certificate template settings are still NOT delivered. <!-- sdd-owner: implementation -->
- [x] 6.t1 Certificate template domain made real (the domain half of 6.f's "certificate template settings"; the management UI is 6.t2): `CourseCertificateTemplateService` owns create/update/activate/deactivate with the rules enforced in the domain — a non-empty `name`; `type_scope` restricted to the three `AcademicDocumentType` values plus the explicit `'*'` wildcard (`null` and unknown scopes refused, and inert on read); `blade_view` restricted to the allowlist of exactly `course-talks.certificates.reference`; `settings_json` restricted to `title`/`intro_text`/`company`/`signatures` with at most two `{name, role}` signatures and unknown keys refused; `html_template` refused as an attribute the domain does not own, so no new path accepts, writes or renders it; a derived `version` (1 on create, +1 per configuration change, unchanged for no-ops and for activation); activation exclusivity per `type_scope` (create, update and activate all sweep, so at most one active template per scope); and the read-only `resolveFor(AcademicDocumentType)`, where a template scoped to the exact type beats the wildcard and the wildcard stays in force for the other types. `CourseDocumentGenerationService` resolves the template once before its transaction, renders the template's `blade_view` with its `settings_json` merged under the caller's `company` default, and persists `course_certificate_template_id`; with nothing active for the type it renders `course-talks.certificates.reference` with the service defaults, unchanged. Evidence: `tests/Feature/Courses/CourseCertificateTemplateTest.php` — 18 tests / 178 assertions passing (baseline 2 / 25); `tests/Feature/Courses/CourseAcademicDocumentGenerationTest.php` — 16 tests / 101 assertions passing (baseline 13 / 71); `--filter=Course` regression 368 tests / 2,801 assertions passing against the 349 / 2,618 baseline, i.e. exactly this unit's 19 new tests / 183 assertions. NOTE: the aggregate 6.f row stays open because certificate template SETTINGS UI (6.t2) and discard (Slice 7) are still NOT delivered, and this unit changes no route, controller, view, policy, permission, enum, seeder or migration. <!-- sdd-owner: implementation -->
- [ ] Review unit 6.t1: the wildcard representation and the specific-beats-wildcard precedence, the per-scope activation-exclusivity mechanism (create, update and activate), the attribute/settings allowlists and the refusal of `html_template`, the derived version semantics, the resolver's read-side allowlist filter for tampered rows, and the unchanged no-template generation path. <!-- sdd-owner: parent -->

- [x] 6.t2 Certificate template management UI (the UI half of 6.f's "certificate template settings"; the domain half is 6.t1): an authenticated surface that lists every template with its name, Spanish scope, derived version, active state and the settings it carries, and that creates, edits, activates and deactivates them — `CourseCertificateTemplateController` plus `CourseCertificateTemplatePolicy::manage` (`course-talks.templates.manage`) are the ONLY ability its seven routes, its two FormRequests and every rendered control ask for, so a control that is offered always opens. The controller is thin: the FormRequests validate shape only and `CourseCertificateTemplateService` owns every rule, so an invalid `type_scope`, an unknown settings key and a third signature each reach the domain and are refused with its own reason, rendered as a visible Spanish message next to the field the domain named and never as a 500; blank optional inputs are dropped before the payload is built so "not configured" is never mistaken for "must be a non-empty string". `blade_view` and `html_template` are kept out of the payload AND out of the view: neither has a form field, neither has a validation rule, the payload is built key by key, and the model no longer mass assigns `html_template` (the `$fillable` fix that closes the risk 6.t1 declared). The access point is a contextual link in the activity list gated by exactly `manage`, not another sidebar entry. Evidence: `tests/Feature/Courses/CourseCertificateTemplateHttpTest.php` — 18 tests / 157 assertions passing; the 6.t1 domain suite stays green (18 / 178) and generation stays green (16 / 101); `--filter=Course` regression 386 tests / 2,958 assertions passing against the 368 / 2,801 baseline, i.e. exactly this unit's 18 tests / 157 assertions. NOTE: the aggregate 6.f row stays open because discard (Slice 7) is still NOT delivered. <!-- sdd-owner: implementation -->
- [ ] Review unit 6.t2: the authorization boundary (`manage` only, one ability across routes, FormRequests and rendered controls), the key-by-key payload construction that excludes `blade_view`/`html_template`, the blank-entry pruning and its list re-indexing, the Spanish refusal mapping by reason tag, the settings map being read from the request input instead of `validated()` (`excludeUnvalidatedArrayKeys` silently swallows an unknown settings key), the removal of `html_template` from `$fillable` together with the generation-suite fixture whose raw-HTML marker is now a no-op, and the access point's gating. <!-- sdd-owner: parent -->

- [x] 6.g Navigation exposure for authorized users only. Evidence: `tests/Feature/Courses/CourseTalksNavigationTest.php` — 11 tests / 133 assertions passing; `--filter=Course` regression 229 tests / 1,439 assertions passing; sidebar entry gated by `viewAny` (`course-talks.view`) and every module screen returns 403 (never 200, never 500) for a user without that permission. <!-- sdd-owner: implementation -->

- [ ] RED: add HTTP feature tests for module index filters, activity CRUD, edition CRUD/state transitions, sessions/teachers, enrollment, attendance matrix, course grade matrix, talks hiding/blocking grades, document generate/regenerate/annul/send/open WhatsApp/confirm/discard, commercial document register/upload/send/discard, and template settings permissions. <!-- sdd-owner: implementation -->
- [ ] GREEN: add authenticated `course-talks` route group and public `/certificate/qr/{token}` route with named routes, middleware, authorization calls, and no route exposure for unauthorized users. <!-- sdd-owner: implementation -->
- [ ] GREEN: implement thin controllers and form requests delegating to services for activities, editions, sessions, participants/enrollments, attendance, grades, academic documents, commercial documents, deliveries, and templates. <!-- sdd-owner: implementation -->
- [ ] GREEN: implement server-rendered Blade views under `resources/views/course-talks` using existing table/badge/alert patterns for lists, forms, details, matrices, delivery modals/actions, and template configuration. <!-- sdd-owner: implementation -->
- [ ] GREEN: add menu/navigation entry for authorized users only after policies and permissions are effective; keep unauthorized users unable to discover or access restricted workflows. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: add tests for validation errors, duplicate activity codes, hybrid address+URL requirements, grade permission denial, revocation permission denial, commercial-document permission denial, and template-management permission denial. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: split oversized views into partials such as `resources/views/course-talks/editions/_form.blade.php`, `_sessions.blade.php`, `_participants.blade.php`, `_delivery-history.blade.php`, and keep each controller action thin. <!-- sdd-owner: implementation -->
- [ ] Run focused verification with `php artisan test --filter=CourseTalks` and any affected controller tests. <!-- sdd-owner: implementation -->
- [ ] Review Slice 6 for UI completeness, authorization coverage, route naming, and adherence to existing Laravel/AdminLTE/Bootstrap patterns. <!-- sdd-owner: parent -->

## Slice 7 — Pending dashboards, alerts, audit reports, rollout controls, full verification

**Depends on:** Slices 1–6. **Edit surfaces:** `app/Services/Courses/CourseAlertService.php`, `app/Jobs/Courses/RefreshCourseDeliveryAlerts.php` if materialized, dashboard controllers/views such as `resources/views/dashboard*.blade.php` or existing admin dashboard paths, `config/courses.php`, `database/seeders/*`, `docs/` only if existing rollout docs require update, `tests/Feature/Courses/CourseDeliveryAlertsTest.php`, `tests/Feature/Courses/CourseAuditTest.php`, `tests/Feature/Courses/CourseRolloutTest.php`.

### Slice 7 units

The slice-level rows below are aggregate and cannot be checked until every Slice 7 unit is delivered (7.a alerts domain, 7.b dashboards, 7.c audit regression, 7.d rollout controls). Track progress through these units instead.

- [x] 7.a Delivery alert domain (`CourseAlertService`) — the domain the dashboards will read, with no dashboard, route, controller or view: `pendingAcademicDocuments()` / `pendingCommercialDocuments()` and their overdue counterparts plus `pendingCount()` / `overdueCount()` are side-effect-free queries whose outstanding predicate is `delivery_status in (pending, failed)` (a failed send still needs the operator; `sent` closed the follow-up by sending and `discarded` closed it explicitly), intersected with document validity — academic `status = current` and QR not revoked (the same rule `hasDeliverableAcademicDocument()` enforces), commercial `status in (registered, sent)` (the pair `hasStreamableCommercialDocument()` accepts). Overdue is the portion of pending whose anchor day is strictly before `today - config('courses.delivery_due_days')`, so a document issued exactly N calendar days ago is still pending and becomes overdue the next calendar day; the cutoff is computed as a date in PHP, which keeps the query driver-portable and makes the comparison calendar-day based rather than hour based. `discard()` closes a follow-up with a required non-empty reason and the responsible actor, appends the `course-delivery-alert-discarded` audit entry with the reason and the previous snapshot, refuses a follow-up already closed by a send, and leaves the document valid — the certificate keeps its `current` status, unrevoked QR token hash and private file, and the comprobante keeps its amount and attachment. Discarding is governed by the existing `course-talks.documents.send` permission through the `send` ability both document policies already define (no new permission: `revoke` guards annulment, which discard explicitly does not do, and `commercial-documents.manage` exists for one channel only). Unit A closes the academic/commercial asymmetry with ONE additive migration (`2026_08_26_000006_add_delivery_discard_reason_to_course_commercial_documents.php`, nullable `text` mirroring the academic column, non-destructive `down()`) and the matching `$fillable` attribute. Evidence: `tests/Feature/Courses/CourseDeliveryAlertsTest.php` — 15 tests / 134 assertions passing; `--filter=Course` regression 401 tests / 3,093 assertions passing against the 386 / 2,959 baseline, i.e. exactly this unit's 15 tests / 134 assertions; `--filter=CourseCommercialDocument` 80 / 718 and `--filter=CourseCertificateQrSecurityTest` 15 / 182 unchanged. NOTE: every aggregate Slice 7 row stays `[ ]` — this unit delivers the alert DOMAIN only; dashboards (7.b), audit regression (7.c) and rollout controls (7.d) are NOT delivered. <!-- sdd-owner: implementation -->

- [x] 7.b Delivery alert dashboards and the filterable alert list (unit 7.b) — wires 7.a's domain into the two surfaces and adds the operator's screen: the main dashboard gains a Cursos y charlas card with the PENDING and OVERDUE counts and a link to the module's own alert list, rendered ONLY for a viewer who can open the module (the visibility decision and the counts live in `DashboardService`, matching the repo's aggregate-owner pattern; a viewer without `course-talks.view` gets `null`, so no card, no count and no link is rendered for them, and the counts are `CourseAlertService::pendingCount()`/`overdueCount()` — the domain's own numbers, never a row count and never a re-derived rule); `GET course-talks/alerts` (`alerts.index`, gated by `CourseActivityPolicy::viewAny`, the ability the module's read surfaces and the sidebar entry already use) lists the outstanding academic documents AND comprobantes in one table with, per row, the activity, edition, participant (or the group payer for a group comprobante), responsible user, document/comprobante type, delivery status, the channel of the LAST attempt from the append-only ledger, the due/overdue state and the delivery-history summary, plus the 7.a discard action with its mandatory reason; the undeliverable state is surfaced by reading `CourseDocumentDeliveryService::hasDeliverableAcademicDocument()`/`hasStreamableCommercialDocument()` in the controller (no `Storage` call and no model query in Blade), so the operator can tell “waiting to be sent” from “cannot be sent until the file is restored” even though the alert domain never checks the file; the eight filters (activity type, edition, participant, responsible, document type, delivery status, last-attempt channel, anchor date range) live in `CourseAlertService::outstandingAcademicDocuments()`/`outstandingCommercialDocuments()`, which START FROM the pending predicate so a filter can never resurrect a closed follow-up nor widen the outstanding set, while the controller only normalizes scalars and reports an unknown value — an array where a scalar is expected is dropped before any query, so no filter value can 500; discard is authorized by the `send` ability 7.a enforces (`course-talks.documents.send`), gated on the rendered control AND on the route, served by TWO document-scoped routes because Laravel binds one model class per route parameter (a single `alerts/{document}/discard` could not resolve both kinds without losing route-model binding and its 404), with every domain rejection rendered as a visible Spanish message and the document left valid. Evidence: `tests/Feature/Courses/CourseDeliveryAlertsDashboardTest.php` — 22 tests / 199 assertions passing (RED was 17 real assertion failures: 404 where 200 was expected, a missing `errors` session key and a `-1` counter against a real `2`, never a PHP fatal); `--filter=CourseDeliveryAlertsTest` 15 / 134 unchanged; `--filter=Dashboard` 40 / 275 passing (the two pre-existing dashboard suites 14 / 44 with zero assertions touched); `--filter=Course` regression 423 tests / 3,292 assertions passing against the 401 / 3,093 baseline, i.e. exactly this unit's 22 new tests / 199 assertions. NOTE: every aggregate Slice 7 row stays `[ ]` — audit regression (7.c) and rollout controls (7.d) are still NOT delivered. <!-- sdd-owner: implementation -->

- [x] 7.c Audit regression suite (`tests/Feature/Courses/CourseAuditTest.php`, new — the audit half of Slice 7's RED row; the dashboard half was already delivered by 7.a/7.b). Every material change the Slice 7 audit row enumerates is asserted against what the spec requires of it: the entry EXISTS, it is named under the `course-*` convention (a model-backed entry through its `course-*` description, a service-written entry through its `course-*` event name), it names the RESPONSIBLE ACTOR, and a modification carries the old and the new value. Coverage: activity code change, edition state change, enrollment, payment completion, attendance creation and correction, grade correction (who/when/previous/new plus the affected participant and edition), result recalculation, document generation, annulment, regeneration, template configuration change, commercial registration, commercial attachment/replacement, academic email attempt (sent and failed), commercial email attempt, academic and commercial WhatsApp confirmations, alert discard, and the QR-token privacy rule. The suite uncovered TWO real defects and one asymmetry, all fixed where the rule belongs: (1) `course_attendances` was the ONLY model-backed course change with no audit trail at all (`CourseAttendance extends Model`, no `LogsActivity`) — now audited under the SAME convention, which moved to `CourseModel::courseActivitylogOptions()` so `course-*` has one source; (2) the explicit `$actor` every course service already receives never reached the activity causer, because `LogsActivity` resolves the causer from the authenticated session — a service called with no session recorded `causer_id = NULL` and, with another user authenticated, recorded the WRONG user. The rule now lives in `CourseAuditActor::asActor()` (`app/Services/Courses/CourseAuditActor.php`), which sets Spatie's causer for exactly one domain write and always clears it, applied by `CourseAttendanceService::mark()`, `CourseGradeService::record()`, `CourseDocumentGenerationService::generate()/regenerate()` (including the deferred `afterCommit` storage write), `CertificateQrTokenService::revoke()`, `CourseCommercialDocumentService::register()/upload()`, `CourseAlertService::discard()` and the four delivery writes; (3) `sendCommercialEmail()` audited nothing while its academic twin wrote `course-document-email-sent`/`-failed`, so the commercial direct path now writes the same two entries. Privacy assertion: the trail records the HASH (`qr_token_hash`, the HMAC-SHA256 of the token) and the RAW token appears in NO column of ANY activity row, with no private `course-academic-documents/` path, no `signature=` link and no recipient in any payload. Evidence: `tests/Feature/Courses/CourseAuditTest.php` — 24 tests / 104 assertions passing; RED was 18 real failures (2 for the missing attendance trail, 15 for the actor, 1 for the missing commercial attempt entry) and 6 scenarios were already green (activity code, edition state, enrollment, payment, template, private-path privacy); `--filter=Course` regression 447 tests / 3,396 assertions passing against the 423 / 3,292 baseline, i.e. exactly this unit's 24 tests / 104 assertions; full suite 1250 tests with exactly the 11 documented external failures and the same 12 pre-existing errors the baseline summary folded into its 1,189 total. NOTE: every aggregate Slice 7 row stays `[ ]` — rollout controls (7.d) are still NOT delivered. REPORTED, NOT FIXED: the queued-email terminal snapshot is written by `app/Jobs/V2/SendEmailMessage::syncCourseDelivery()` with no actor, and that job is outside this unit's edit surfaces; `course_edition_teachers` cannot be audited at model level (no `id` column, no audit columns, `$timestamps = false`), so teacher changes have no activity entry and teacher changes are not in the enumerated list. <!-- sdd-owner: implementation -->
- [x] 7.d Rollout seeding and rollback controls (unit 7.d — the GREEN rollout row and the TRIANGULATE rollback row). The defect: `CoursePermissionsSeeder` existed and only tests ran it, so `DatabaseSeeder` never created the 13 `course-talks.*` permissions — a fresh `php artisan db:seed` left the module unreachable for every non-admin role (the sidebar entry is gated on `viewAny` → `course-talks.view`, and every module route answers 403). `DatabaseSeeder` now calls it after `SupportPermissionsSeeder` and before `AdminUserSeeder` (it MUST run after `RolesAndPermissionsSeeder`, which syncs the roles, or the grants would be wiped) and stays idempotent (`firstOrCreate` + merge-then-sync). `CoursePermissionsSeeder` itself is unchanged: admin holds all 13 and supervisor holds `course-talks.view` (read-only) — the seeder's explicit grant is the shipped intent (the design specifies no broader supervisor grant, so inventing one would be a product decision). Rollback guidance lives as a comment on the seeding path in `DatabaseSeeder`, not as a new document. Evidence: `tests/Feature/Courses/CourseRolloutTest.php` — 9 tests / 92 assertions passing (RED was 4 failures + 3 errors against the un-wired seed: the permission absent, a supervisor getting no sidebar entry, 0/13 module permissions, and `revokePermissionTo('course-talks.view')` throwing `PermissionDoesNotExist`); `--filter=SeedersTest` 2 / 31 (count moved 130 → 143 in two places — the 13 now really-seeded rows); `--filter=RolesAndPermissionsTest` 10 / 69 unchanged (it seeds `RolesAndPermissionsSeeder` only, so its 90/107/70/82 do NOT move); `--filter=Course` 456 tests / 3,488 assertions passing against the 447 / 3,396 baseline, i.e. exactly this unit's 9 tests / 92 assertions; `--filter=CourseCertificateQrSecurityTest` 15 / 182 unchanged; full suite 1259 tests with exactly the 11 documented external failures and the 12 pre-existing campaign errors (no new failures; 1250 + 9). Rollback controls proven: permission removal hides the routes AND the menu entry and deletes nothing (rows, private files, QR token and audit rows survive; re-granting restores access); a QR link stays live through hiding access and through discarding a delivery follow-up and is revoked ONLY by an explicit annulment; the only eligibility job (`EvaluateCourseDocumentEligibility`) is a documented no-op (writes no document and no file) so there is no asynchronous generation to stop. REPORTED, NOT CHANGED: the seeded ADMIN still reaches the module through the role-based `Gate::before` bypass in `AuthServiceProvider`, so hiding it from admin is a product decision, not a permission-only rollback. NOTE: every aggregate Slice 7 row stays `[ ]`.

- [ ] RED: add tests for pending and overdue counts after one calendar day, configurable due days, module-dashboard filters by activity type/edition/participant/responsible/document type/status/channel/date, alert discard with reason, and close only by send/discard. <!-- sdd-owner: implementation -->
- [ ] RED: add audit regression tests for activity code changes, edition state changes, enrollment changes, attendance changes, grade corrections, result recalculations, payment changes, document generation/annul/regeneration, template changes, commercial document changes, delivery attempts, WhatsApp confirmations, and alert discard. **DELIVERED as unit 7.c** (see the `7.c` row under `### Slice 7 units`); this aggregate row stays `[ ]` because it is the slice-level label, not because work is missing. <!-- sdd-owner: implementation -->
- [ ] GREEN: implement `CourseAlertService` computed pending/overdue queries or materialized alerts only if required, using default `delivery_due_days=1` from `config/courses.php` and preserving document validity when delivery follow-up is discarded. <!-- sdd-owner: implementation -->
- [ ] GREEN: wire main dashboard and module dashboard counters/lists with filters and links to pending certificates/commercial documents without exposing private data to unauthorized roles. <!-- sdd-owner: implementation -->
- [ ] GREEN: ensure all material changes emit Spatie activity log entries with `course-*` event names, old/new values where required, responsible actor, and no raw QR tokens. <!-- sdd-owner: implementation -->
- [ ] GREEN: add rollout seeding/assignment path for permissions and menu enablement, leaving safe rollback guidance in code comments/config only where existing project conventions support it. **DELIVERED as unit 7.d** (see the `7.d` row under `### Slice 7 units`); this aggregate row stays `[ ]` because it is the slice-level label, not because work is missing. <!-- sdd-owner: implementation -->
- [ ] TRIANGULATE: test rollback controls: hiding route/menu by permissions, stopping eligibility jobs through queue/config if introduced, preserving rows/files, and revoking QR links only through explicit document annul/replacement paths. **DELIVERED as unit 7.d** (see the `7.d` row under `### Slice 7 units`); this aggregate row stays `[ ]` because it is the slice-level label, not because work is missing. <!-- sdd-owner: implementation -->
- [ ] REFACTOR: consolidate dashboard query scopes on models/services and remove duplication between main dashboard and module dashboard. <!-- sdd-owner: implementation -->
- [ ] Run full verification with `php artisan test` and confirm NO NEW failures beyond the documented external baseline in `openspec/changes/course-talks-management/suite-baseline.md` (11 pre-existing failures owned by the in-flight B12/B14, settings and email/calendar work — proven against `main`, not attributable to this change), then rerun the module's focused tests. A whole-suite-green claim is NOT reachable while those 11 stay open, so this row asserts "no regressions against the documented baseline" plus a green `--filter=Course`. <!-- sdd-owner: implementation -->
- [ ] Review Slice 7 for alerts, audit completeness, rollout safety, and full-test evidence before archive/next SDD gate. <!-- sdd-owner: parent -->

## Cross-slice guardrails

- [ ] Keep historical Excel import, SUNAT/accounting-provider integration, payment gateway, automatic WhatsApp API document delivery, public enrollment/student portal, weighted formulas, videoconference/content platform, and third-party digital signature integration out of v1. <!-- sdd-owner: implementation -->
- [ ] Keep tests with the slice that implements the behavior; do not create a separate tests-only PR unless it is a deliberate RED-only review unit approved by the parent. <!-- sdd-owner: implementation -->
- [ ] Preserve private storage for generated/uploaded documents and never expose `storage:link` public paths for academic or commercial documents. <!-- sdd-owner: implementation -->
- [ ] Track approximate changed lines after each slice with `git diff --stat` if the repository is initialized; pause when a slice approaches the selected review-budget policy. <!-- sdd-owner: implementation -->

## Corrective unit — close the commercial delivery cycle and stop duplicate certificate generation

Scope: two verified P0 defects found by reading the code after 6.f-2b. Both are corrective to already-delivered behavior; no aggregate row (`6.e`, `6.f`) is marked complete by this unit. Tracked separately and explicitly NOT addressed here: the commercial registration idempotency key, the commercial WhatsApp handoff race recovery, the bypassable zero guard, the false regenerate message, the offer-to-send-without-file, the stale comment in `AnnulAcademicDocumentRequest`, and all deduplication work.

- [x] RED/GREEN: Defect A — a commercial email delivery never reached a terminal state. `SendEmailMessage::syncCourseDelivery()` is the only writer of the terminal ledger status and of the `delivery_status` / `last_sent_at` snapshot, and it returned early for every `related_entity_type` that was not `CourseAcademicDocument`, so a confirmed commercial send stayed `queued` forever and a failed one recorded no error anywhere. The synchronization now maps the message state to the ledger outcome once and applies it to whichever course document the row correlates to (`CourseAcademicDocument` or `CourseCommercialDocument`); every other correlated entity keeps the previous no-op. Academic semantics are preserved exactly (sent / failed / unconfirmed → `queued` + "no se pudo confirmar" error with the snapshot left pending). Evidence: `tests/Feature/Email/SendEmailMessageCorrelationTest.php` — 8 tests / 50 assertions passing; RED was `'sent'` vs `'queued'` and `'failed'` vs `'queued'` plus a `null` unconfirmed ledger error. <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Defect B — a repeated generate created a second current certificate. `CourseDocumentGenerationService::generateDocument()` now refuses, before any side effect, to mint a second current document for the same enrollment, and is exempt only for regeneration (which announces the replacement with its callback and remains the legitimate path to a new current document). The refusal is the Spanish message `La matrícula ya cuenta con un documento académico vigente. Para emitir uno nuevo, regenere el documento vigente indicando el motivo.`, surfaced verbatim by the existing `InvalidArgumentException` mapping in `CourseAcademicDocumentController::store()` (never a 500). The Blade presentational guard (`$canGenerate && $current === null && $result->eligible`) is deliberately kept as defence in depth. Evidence: `tests/Feature/Courses/CourseAcademicDocumentGenerationTest.php` — 12 tests / 62 assertions passing; `tests/Feature/Courses/CourseAcademicDocumentHttpTest.php` — 17 tests / 159 assertions passing; RED was `Failed asserting that 2 is identical to 1` on the current-document count plus a missing `documents` error bag. <!-- sdd-owner: implementation -->
- [x] Shared-infrastructure regression lock: a delivery correlated to an entity that is not a course document (quotation path) still gets the pre-existing no-op treatment. Evidence: `SendEmailMessageCorrelationTest::test_a_delivery_correlated_to_a_non_course_entity_is_left_untouched`. <!-- sdd-owner: implementation -->
- [x] Verify: `--filter=Course` regression — 331 tests / 2,471 assertions passing against the 329 / 2,456 baseline (this unit adds exactly its 2 new Course-side tests). Shared-job consumers: `--filter=QuotationGmailSendTest` 10 / 41 passing, `tests/Feature/Email` + `NotificationsTest` 29 / 95 passing with one PRE-EXISTING unrelated failure (`GmailProviderTest::test_send_returns_documented_error_envelope_when_credentials_missing` expects `NotImplementedException` while the untouched `GmailProvider` now returns `NoBoundAccount`; outside this unit's allowed surfaces). <!-- sdd-owner: implementation -->
- [ ] Review this corrective unit: terminal-state synchronization for both course document types, the generate refusal and its Spanish message, the preserved Blade guard, and the untouched non-course consumers. <!-- sdd-owner: parent -->

## Corrective unit R1 — commercial registration idempotency and the commercial WhatsApp handoff race

Scope: two P1 findings from an independent review of the commercial delivery cycle, both corrective to already-delivered behavior. No aggregate row (`6.e`, `6.f`) is marked complete by this unit. Tracked for a following unit and explicitly NOT addressed here: the bypassable group zero guard, the false regenerate message, the delivery controls shown without a file, the stale comment in `AnnulAcademicDocumentRequest`, and all academic/commercial deduplication.

- [x] RED/GREEN: Defect A — a double submit of the commercial registration form duplicated the financial document. `resources/views/course-talks/editions/commercial-documents.blade.php` now carries a hidden per-render `operation_key` in BOTH registration forms (single enrollment and group purchase), minted once per rendered form exactly like the three delivery forms; the key is persisted in the new nullable unique `course_commercial_documents.idempotency_key` column (migration `2026_08_26_000005_add_course_commercial_document_idempotency_key.php`); `CourseCommercialDocumentService::register()` returns the document an operation already registered instead of writing a second one, and catches the concurrent-insert `QueryException` to resolve to the row the other request committed, mirroring the delivery channel. Without a key the behavior is byte-for-byte the pre-idempotency one, so an external/legacy registration path is unaffected. Evidence: `CourseCommercialDocumentRegistrationTest` — 16 tests / 72 assertions passing; `CourseCommercialDocumentHttpTest` — 22 tests / 288 assertions passing (RED: `Failed asserting that table [course_commercial_documents] matches expected entries count of 1. Entries found: 2`). <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Defect B — the commercial WhatsApp handoff had no key-collision recovery. `CourseDocumentDeliveryService::openCommercialWhatsAppHandoff()` and `confirmCommercialWhatsAppSent()` now catch `QueryException` and return the already-registered ledger row, exactly as `openAcademicWhatsAppHandoff()`/`confirmAcademicWhatsAppSent()` do; public signatures and the existing guard order are unchanged. Evidence: `CourseCommercialDocumentDeliveryTest` — 19 tests / 130 assertions passing (RED: the unique-constraint exception escaped as `SQLSTATE[23000] ... UNIQUE constraint failed: outbound_deliveries.idempotency_key`, the HTTP 500 the delivery controller produces because it only catches `InvalidArgumentException`). <!-- sdd-owner: implementation -->
- [x] Migration safety: `2026_08_26_000005_add_course_commercial_document_idempotency_key.php` adds one nullable `CHAR(64)` column plus the named unique index `uq_course_commercial_documents_idempotency_key`; `down()` drops the index and then the column. Additive and non-destructive: verified on the test connection (sqlite `:memory:`) that the column and the strictly-UNIQUE single-column index exist, that three rows with a `NULL` key coexist under it (a unique index treats NULLs as distinct, so every existing row is valid), and that a duplicate non-null key is rejected; `migrate:rollback --step=1` removed the column/index while the persisted documents and their amounts survived. <!-- sdd-owner: implementation -->
- [x] Verify: `--filter=Course` regression — 341 tests / 2,559 assertions passing against the 332 / 2,478 baseline (this unit adds exactly its 9 new Course-side tests: 4 + 3 + 2). `--filter=CourseCommercialDocument` 78 / 706 passing; shared-infrastructure neighbour `--filter=SendEmailMessageCorrelationTest` 8 / 50 passing. <!-- sdd-owner: implementation -->
- [ ] Review this corrective unit: the nullable-unique-column design and its migration, the replay and concurrent-collision resolution in `register()`, the per-render key in both registration forms, the mirrored WhatsApp recovery, and the untouched aggregate rows and out-of-scope findings. <!-- sdd-owner: parent -->

## Corrective unit R2a — group money authority, real regeneration reason, deliverable-only controls, truthful annul comment

Scope: the four functional minor findings of an independent review of the delivered slices, all corrective to already-delivered behavior. No aggregate row (`6.e`, `6.f`) is marked complete by this unit, and every previously existing row is unchanged. Tracked for a following unit and explicitly NOT addressed here: the academic/commercial duplication, the `mailOperation` stub and the `sendAcademicEmail`/`sendCommercialEmail` pair, the certificate template settings, the `discard` action, and the pre-existing suite failures.

- [x] RED/GREEN: Finding 1 — the group zero-total guard was bypassable. `CourseCommercialDocumentService::register()` evaluated the explicit-`subtotal_amount` branch BEFORE the group branch, and `calculate()` accepts a zero subtotal, so a group target carrying that key could override or zero the group's money and reopen the hole `a8cf694` closed. The group branch is now evaluated first: a group target's money is always `calculateGroupCharges()` over its own billable enrollments, and a zero aggregation is still refused in Spanish instead of being persisted. An explicit `subtotal_amount` is ignored for a group target (decision and justification in `apply-progress.md`) and still wins for an enrollment target, whose Slice 4 behavior is unchanged. Evidence: `tests/Feature/Courses/CourseCommercialDocumentRegistrationTest.php` — 18 tests / 80 assertions passing; RED was `'120.00'` vs `'200.00'` and `'120.00'` vs `'0.00'`. <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Finding 2 — regenerate reported a false reason. The regenerate surface mapped every `InvalidArgumentException` to one constant, so a regeneration refused for eligibility was told it was refused for not being current. `CourseDocumentGenerationService` now throws the typed `App\Exceptions\Courses\InvalidCourseDocumentState` (a tag per refusal: `NOT_CURRENT`, `NOT_ELIGIBLE`, `CURRENT_ALREADY_EXISTS`) and `CourseAcademicDocumentController::regenerationRejection()` picks the sentence from the tag, never from message text; the eligibility refusal keeps its own Spanish wording verbatim and an unclassified failure gets a fallback that claims no cause. The annul path keeps its `catch` and its status re-read unchanged. Evidence: `tests/Feature/Courses/CourseAcademicDocumentHttpTest.php` — 20 tests / 180 assertions passing; `tests/Feature/Courses/CourseAcademicDocumentGenerationTest.php` — 13 tests / 71 assertions passing; RED was the page containing no real reason plus `'InvalidArgumentException'` where the tagged class was expected. <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Finding 3 — delivery controls were offered for a document that cannot be delivered. The two listing views gated the controls on the document status only, while the service predicate also requires the private file to exist, so an undeliverable document was offered an action guaranteed to be refused. `CourseDocumentDeliveryService::hasDeliverableAcademicDocument()` and `hasStreamableCommercialDocument()` are now public side-effect-free queries (the rule still lives in exactly one place, and the delivery behaviour is unchanged); `CourseAcademicDocumentController::index()` and `CourseCommercialDocumentController::index()` pass a per-document verdict the views render, keeping the existing presentational status guards as defence in depth and the service rejection path intact for stale or tampered requests. No `Storage` or model query was added to Blade. Evidence: `tests/Feature/Courses/CourseAcademicDocumentDeliveryHttpTest.php` — 17 tests / 195 assertions passing; `tests/Feature/Courses/CourseCommercialDocumentDeliveryHttpTest.php` — 18 tests / 211 assertions passing; RED was a rendered `…-email-form-{id}` for a current/registered document without its private file. <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Finding 4 — a false comment. `AnnulAcademicDocumentRequest`'s docblock claimed "the service itself has no such guard", false at HEAD since `39596f2`: `CertificateQrTokenService::revoke()` re-reads the persisted status under `lockForUpdate()` inside a transaction and refuses anything that is not current. The comment now states what the code does — the controller check is defence in depth alongside that guard, which remains the authority. Evidence: `tests/Feature/Courses/CourseAcademicDocumentHttpTest.php::test_the_annul_form_comment_matches_the_service_guard_that_actually_exists` (asserts the service refuses on its own AND that the comment no longer denies it); RED failed on the old sentence. <!-- sdd-owner: implementation -->
- [x] Verify: `--filter=Course` regression — 349 tests / 2,618 assertions passing against the 341 / 2,559 baseline (this unit adds exactly its 8 new implementation-owned tests). Six focused filters run sequentially in the brief's order. <!-- sdd-owner: implementation -->
- [ ] Review this corrective unit R2a: the ignore-vs-refuse decision for the group target, the typed-exception mapping and its fallback, the single-sourced deliverability verdict in both listings, the corrected comment's claim, and the untouched aggregate rows and out-of-scope findings. <!-- sdd-owner: parent -->

## Corrective unit — foundation review (destructive document deletion, schema rollback, orphan permissions)

Scope: the two defects and five mechanical findings of the independent foundation review of the delivered slices. All corrective to already-delivered behavior; no aggregate row (`6.e`, `6.f`, Slice 7) is marked complete by this unit. Explicitly NOT addressed here: the academic/commercial deduplication, the `mailOperation` stub, the audit-job blocker, the two unapplied migrations, the 11 external failures, and the two raw status columns' missing enums (documented in `known-limitations.md`).

- [x] RED/GREEN: Defect A — deleting a document that a course document still references destroyed the private file first and then died on the implicit RESTRICT foreign key of `course_academic_documents.document_id` / `course_commercial_documents.document_id`. `DocumentService::delete()` now (1) refuses with a Spanish `ConflictHttpException` (HTTP 409) when either course table still points at the row and (2) removes the DB row inside a transaction BEFORE the file, so a file is only ever destroyed once nothing references it. Reference check uses `withTrashed()` because a soft-deleted course document still holds the foreign key. Evidence: `tests/Feature/DocumentServiceTest.php` — 9 tests / 43 assertions passing (RED was the file already destroyed: `El archivo privado del certificado NO debe destruirse…` plus an uncaught FK `QueryException`); `tests/Feature/DocumentHttpTest.php` filter — 49 tests / 491 assertions passing (RED was `Expected response status code [409] but received 500`, the FK `PDOException` in the response). Two mutations proved the new assertions bite: dropping `withTrashed()` from the guard kills only the soft-deleted triangulation test, and the pre-fix file-first order is exactly what the RED run measured (the file already destroyed). The happy path stays proven: the unreferenced lead document and the superseded comprobante attachment are still deleted with their files. <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Defect B — the schema rollback was undocumented. `known-limitations.md` now records that `migrate:rollback` drops all 12 domain tables while (a) the private PDFs stay in `storage/app/private/docs` with no surviving row to locate them and (b) the surviving `documents` rows can end up pointing at a DIFFERENT certificate once `AUTO_INCREMENT` is reused. `DatabaseSeeder`'s docblock was NOT touched: it documents the SERVICE rollback and is correct there. Evidence: `CourseDomainFoundationTest::test_the_schema_rollback_damage_is_documented` (RED: the file contained no `migrate:rollback` / `AUTO_INCREMENT` / `storage/app/private/docs`). <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Finding 1 — the seeded `course-talks.sessions.manage` permission enabled nothing (only `CourseEditionPolicy::manageSessions`, which nothing invoked; the session routes authorized with `editions.manage`). Both session routes are now authorized by `manageSessions` (`CourseEditionController::sessions()`/`syncSessions()`), and `SyncEditionSessionsRequest::authorize()` mirrors the policy so the form cannot refuse a user the route would admit. The policy keeps the permission ADDITIVE (`sessions.manage` OR `editions.manage`): the dedicated permission is now the only thing a non-edition-manager needs, while the strict variant would have revoked session access from every `editions.manage`-only actor and required three files outside this unit's surfaces (`CourseEditionSessionsHttpTest`, `CourseTalksNavigationTest`, `editions/show.blade.php`). `course-talks.audit.view` was NOT invented a surface: it is documented as an open decision in `known-limitations.md`. Evidence: `CoursePermissionPolicyTest::test_the_sessions_manage_permission_is_consumed_by_the_session_surface` (RED: `Expected response status code [200] but received 403` for a user holding `sessions.manage` without `editions.manage`). <!-- sdd-owner: implementation -->
- [x] RED/GREEN: Finding 2 — `CoursePermissionPolicyTest` did not isolate the clause it claimed to test. The responsible user no longer holds `course-talks.view`, so `CourseEditionPolicy::view`'s ownership clause is the only reason the assertion passes, and the missing negative case (a non-responsible user with no permission) is added alongside a positive case for the permission half of the clause. Evidence: with the ownership clause deleted, the new test FAILS (and, measured the same way, the old fixture stayed GREEN — the mutation survived the old test). <!-- sdd-owner: implementation -->
- [x] Finding 3 — the 13-permission list is no longer duplicated in the policy test: it uses `CoursePermissionsSeeder::PERMISSIONS` (the constant the rollout suite already reuses). Test-only change, no behavior to observe; the local literal is gone. <!-- sdd-owner: implementation -->
- [x] Finding 4 — `CourseDomainFoundationTest` now covers what its name claims: all 12 tables the migration creates (`course_edition_teachers` added) and all 4 `config/courses.php` keys (`email_document_link_minutes` = 10080 added). Coverage gap, not a production defect: the new assertions pass against the unchanged schema, which is exactly why the old test could not catch a regression here. <!-- sdd-owner: implementation -->
- [x] Finding 5 — `course_commercial_documents.status = 'sent'` is read in three places and written by nothing in `app/` (grep evidence: writers are `pending_file`/`registered` in `CourseCommercialDocumentService`; readers are `PublicCertificateQrController`, `CourseAlertService::SERVABLE_COMMERCIAL_STATUSES`, `CourseDocumentDeliveryService::hasStreamableCommercialDocument`). The read is KEPT, deliberately: it is a persisted status the design declares and a future/external writer's comprobante must stay streamable. No writer was invented; the reservation is documented in `known-limitations.md`. Evidence: `CourseDomainFoundationTest::test_the_reserved_commercial_status_is_documented` (RED: the file contained no `course_commercial_documents.status` / `reserved`). <!-- sdd-owner: implementation -->
- [x] Verify: `--filter=Course` regression — 463 tests / 3,527 assertions passing against the 456 / 3,488 baseline (this unit adds exactly its 7 matched tests: 3 document-service + 1 document-HTTP + 1 policy + 2 foundation). Focused filters run sequentially in the brief's order: `DocumentServiceTest` 9 / 43, `DocumentHttpTest` (matches the course document HTTP suites too) 49 / 491, `CoursePermissionPolicyTest` 4 / 66, `CourseDomainFoundationTest` 6 / 30, `CourseRolloutTest` 9 / 92 (unchanged), `CourseCertificateQrSecurityTest` 15 / 182 (unchanged). Full suite 1267 tests / 6613 assertions with exactly the 11 documented external failures and the 12 pre-existing campaign errors — no new failures (1259 + 8 = 1267). <!-- sdd-owner: implementation -->
- [ ] Review this corrective unit: the fail-closed reference guard and the row-before-file order, the 409 surfacing without a controller change, the additive `manageSessions` decision and its authorization consequence, the two documented open decisions (`audit.view`, reserved `sent`), the corrected policy-test fixture, the completed foundation coverage, and the untouched aggregate rows and out-of-scope findings. <!-- sdd-owner: parent -->
